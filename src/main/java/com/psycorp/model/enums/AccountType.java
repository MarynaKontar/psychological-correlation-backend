package com.psycorp.model.enums;

/**
 * Types of test result's comparison between users.
 * @author Maryna Kontar
 */
public enum AccountType {
//    /* if user invite another user or was invited */
//    INVITE,
    /* if the user has opened his profile for comparison with other users */
    OPEN,
    /* if the user has closed his profile for comparison with other users*/
    CLOSE
}
