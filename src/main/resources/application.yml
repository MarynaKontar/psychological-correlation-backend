
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  data:
    mongodb:
      host: ${DB_HOST:localhost}
      port: ${DB_PORT:27018}
      database: psymatch
      username: mongodb_admin
      password: MiVpp1g
      authentication-database: admin
#при пуше на git, убрать комментарии и сменить порт на 27017
